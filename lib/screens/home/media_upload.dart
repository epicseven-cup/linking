import 'package:flutter/cupertino.dart';

class MediaUpload extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(child: Container(child: Text('File / Text')));
  }
}
